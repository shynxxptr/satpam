# Core dependencies with version ranges for safety
# Allows patch/minor updates but blocks major breaking changes
discord.py>=2.3.0,<3.0.0  # Allow 2.x updates, block 3.x breaking changes
python-dotenv>=1.0.0,<2.0.0

# Music features (optional - uncomment if needed)
# yt-dlp>=2023.12.30
# PyNaCl>=1.5.0
# spotipy>=2.23.0  # For Spotify API support
# psutil>=5.9.0  # For resource monitoring

